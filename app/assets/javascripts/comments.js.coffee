# Place all the behaviors and hooks related to the matching controller here.
# All this logic will automatically be available in application.js.
# You can use CoffeeScript in this file: http://coffeescript.org/



# (document).on "page:change", ->
#  $(".form-horizontal").on("ajax:success", (e, data, status, xhr) ->
#    @comment_text = $(this).find( "textarea" ).val()
#    @user_name = $(this).find( "textarea" ).attr('data')
#    $(".comments").append("<div class='comment clearfix'>
#                              <p class='comment-author pull-left'>#{@user_name} :</p>
#                              <p>#{$(@comment_text).val()}</p>
#                              <a class='edit-comment pull-right'>Редактировать</a>
#                              <a class='delete-comment pull-right' data-remote='true' data-method='delete' href='#'>Удалить</a>
#                            <div>")
#  ).on "ajax:error", (e, xhr, status, error) ->
#    $(".comments").append "<p>ERROR</p>"


# $(".delete-comment").on "ajax:success", (e, data, status, xhr) ->
#   current_comment = $(this).parents('div')[0];
#   $(current_comment).fadeToggle(300);


