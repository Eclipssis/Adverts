%section.middle.container
  %h2.text-center Пользователи
  .col-lg-6.col-md-6.col-sm-8.col-xs-12.col-lg-offset-2.col-md-offset-3.col-sm-offset-2
    %table.table.table-bordered.users
      %tr
        %th Логин
        %th Роль
        %th Изменить роль
      - @users.each do |user|
        %tr
          %td
            = link_to( user.login, user_profile_path(user))
          %td
            %span= user.role.name
          %td
            = form_for( user , url: admin_change_role_path, html: { method: :post } ) do |f|
              .select-form
                = f.select :role_id, @roles.map{ |role| [role.name, role.id] }, {}, { selected: user.role.id, autofocus: 'true', class: 'form-control input-sm' }
              = f.hidden_field :id
              = f.submit "Изменить роль", class: 'btn btn-xs btn-success'