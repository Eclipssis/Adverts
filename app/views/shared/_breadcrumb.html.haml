%ol.breadcrumb.col-lg-3.col-md-3.col-sm-3.col-xs-12
  - breadcrumb.each_with_index do |(href,title), index|
    - if breadcrumb.size - 1 == index
      %li.active=title
    - else
      %li
        %a{:href => href}=title


